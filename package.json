{
  "name": "internetcafe-influencer",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "description": "One-phone TouchDesigner JPEG/WebSocket roundtrip prototype.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --env-file-if-exists=.env --watch server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.0",
    "qrcode": "^1.5.4",
    "ws": "^8.18.1"
  }
}
